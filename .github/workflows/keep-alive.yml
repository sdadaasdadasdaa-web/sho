name: Keep Alive

on:
  schedule:
    - cron: '*/25 * * * *'  # every 25 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: curl -s -o /dev/null -w "%{http_code}" https://shopachadinhos-sho.hf.space/
